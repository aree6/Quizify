# Netlify root config for monorepo — points to frontend/
[build]
  base = "frontend/"
  command = "npm run build"
  publish = "frontend/dist/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
